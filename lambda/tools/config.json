{
  "AUTHZ_HOST": "${AUTHZ_HOST}",
  "AUTHZ_PORT": "${AUTHZ_PORT}",
  "AUTHZ_PATH": "${AUTHZ_PATH}",

  "AWS_REGION": "us-east-1",
  "AWS_SECRET_ARN_OIDC": "${AWS_SECRET_ARN_OIDC}",
  
  "KEEP_ALIVE_TIMEOUT": 5000
}
